
AWSTemplateFormatVersion: "2010-09-09"
Metadata:
    Generator: "https://github.com/meta-introspector/typescript-zos-introspector"
Description: "parameters extracted from https://github.com/sekmet/synapzeai"
Parameters:

  pgliteDataDir:
    Type: 'String'
    Description: 'Parameter for PGLITE_DATA_DIR'
    
Conditions:
  HasPgliteDataDir: !Not [!Equals [!Ref pgliteDataDir, ""]]
Resources:

  AgentParamPgliteDataDirParameter:
    Type: 'AWS::SSM::Parameter'
    Condition: HasPgliteDataDir
    Properties:
      Name: !Sub '${AgentCodeName}_PGLITE_DATA_DIR'
      Type: 'String'
      Value: !Ref pgliteDataDir
      Tier: 'Standard'
      Description: 'Parameter for PGLITE_DATA_DIR'
